# Settings for Backend (on Cloud Run).
# See https://firebase.google.com/docs/app-hosting/configure#cloud-run
runConfig:
  minInstances: 0
  maxInstances: 2
env:
  - variable: GEMINI_API_KEY
    secret: GEMINI_API_KEY
  - variable: CHARACTER
    value: "Talk like someone that has had too much coffee."
    availability:
      - BUILD
      - RUNTIME
